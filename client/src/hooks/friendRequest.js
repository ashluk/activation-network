import { useState, useEffect } from "react";

export function Friendrequest() {
    //we will need to recieve some props.
    //we need to know whose profile button is being rendered on, the parent component otherUserProfile or whatever...
    //this.props.match.params.id is what we need to see the other id
}
